class Solution {
    public int[] buildArray(int[] nums) {
        int[] ans = new int[nums.length];
        for(int val = 0; val < nums.length; val++){
            ans[val] = nums[nums[val]];
        }
        return ans;
        
    }
}
